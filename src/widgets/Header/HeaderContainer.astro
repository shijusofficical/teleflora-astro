---
import HeaderView from '@widgets/Header/Header';
import NavBarContainer from '@widgets/NavBar/NavbarContainer';
const { data, additionalData } = Astro.props;
---
<HeaderView data={data} additionalData={additionalData} client:idle={{timeout: 5000}} />
<NavBarContainer client:media="(max-width: 1200px)" />
